import FloatBackground from "@/components/float-background";

export default function Page() {
    return (
        <div className="h-screen w-screen">
            <FloatBackground />
        </div>
    );
}